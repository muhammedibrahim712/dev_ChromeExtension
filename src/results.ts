import { Cell, Sheet } from './types'

export const updateTerm = (term: string) => {
  console.info(Sheet.Results)
  // check if term exists
  /// if exists, update frequency
  /// check if brand exists in brands
  /// if not, add brand to brands list and update brand count
}
