export const visit = async (tab: string, url: string, focus?: boolean) => {
  // focus if needed
  // navigate to page
  // wait for page to load
}

export const getMetadata = (): string[] => {
  return []
}

export const getTitle = (): string => {
  return ''
}

export const getText = (): string[] => {
  return []
}

export const getLinks = (): string[] => {
  return []
}
